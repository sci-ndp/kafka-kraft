# Repo-specific instructions (keep private)
AGENTS.md

# Entire certs directory or symlink (keeps real material out of repo)
certs

# Environment (contains credentials)
.env
.kafka-credentials

# Generated docker-compose
docker-compose.generated.yml

# Local setup cache
.setup-config

# Kafka data directory
data/
data-user/

# TLS material and certbot outputs (avoid committing real keys/keystores)
certs/*.key
certs/*.pem
certs/*.p12
certs/*.srl
certs/*.csr
certs/live/
certs/letsencrypt/
certs/config/
certs/logs/
certs/work/
certs/certs/
